import { RegisterActorPayload } from './register-actor-payload';

describe('RegisterActorPayload', () => {
  it('should create an instance', () => {
    expect(new RegisterActorPayload()).toBeTruthy();
  });
});
