import { JwtAutResponsePayload } from './jwt-aut-response-payload';

describe('JwtAutResponsePayload', () => {
  it('should create an instance', () => {
    expect(new JwtAutResponsePayload()).toBeTruthy();
  });
});
